{
  "name": "@sage/audit",
  "version": "0.0.1",
  "private": true,
  "description": "Shared DynamoDB audit-trail writer used by every SAGE agent -- one AuditRecord per agent decision.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@sage/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0",
    "vitest": "^5.0.0"
  }
}
