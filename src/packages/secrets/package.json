{
  "name": "@sage/secrets",
  "version": "0.0.1",
  "private": true,
  "description": "Shared runtime secret resolution for SAGE agents: local .env in dev, SSM Parameter Store (SecureString) once deployed",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.600.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  }
}
